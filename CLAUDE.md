# World Mappy — 執筆・生成ガイド

海外情報サイト「World Mappy」の国別ページに関する運用ルール。技術的なビルド手順は `assets/tools/generate.py` のdocstring・`assets/tools/_country_template.json`（新規国のスキーマ雛形）を参照。ここでは**テンプレートのコードが強制しない、執筆上の慣習**をまとめる。

## モデルコース（courses）の設計方針

`courses.stable_plans[]` は配列で、テンプレート的には何個でも・どんな内容でも入れられる。ただしこれまでの全国（マレーシア・タイ・ウズベキスタン・南アフリカ・台湾・シンガポール・スリランカ）で共通して、以下の構成で統一してきた。

- **プラン1（最初の要素）**: 拠点を絞って身軽に周る、初心者・王道向けのプラン。スーツケースの移動が少ない／ホテル連泊が基本。
- **プラン2以降**: プラン1より上級者向け・移動多めのプラン。荷造り・移動の手間が増える分、必然的にバックパッカー気質と相性がいい内容になる。「一都市一泊」「新幹線で縦断」「全都市を横断」のような、移動を伴うルート。
- **`adventure_plan`（`stable_plans`とは別の単数フィールド）**: 「冒険枠」。`stable_plans`の中には入れず、ページ最下部に必ず表示される専用スロット。管理人の実体験ルートである場合もあるが、それが必須条件ではない。実体験がある国はそれをそのまま書き（例: 南アフリカ）、実体験がない国は一般的な「上級者向けの尖ったルート」（例: 台湾の環島、マレーシアの半島縦断）を書けばよい。

**方針**: プラン2をわざわざ「バックパッカー向け」と銘打つ必要はない。移動を増やす設計にすれば自然とその色になるので、無理に尖らせず「プラン1より一段階、行動範囲を広げる」くらいの温度感でよい。台湾の「週末プラン（3泊4日・拠点集中）→縦断プラン（5泊6日・移動多め）→環島（`adventure_plan`・上級者の憧れルート）」という3段構成が理想形。

新しい国のモデルコースを書くときは、この3段階の温度差を意識する。

### 日をまたぐ移動 vs 日帰りの移動

- **日をまたぐ移動**（前日午後に出発／翌日午前に到着など、宿泊地が変わる移動）: 従来通り`{"transit": true, "duration": "...", "train": true}`のような独立した行を日と日の間に挟む。矢印（│▼）付きで表示される。
- **同じ都市に戻ってくる日帰り**（例: KL発→マラッカ→KL、同じ拠点に帰着）: 独立行にせず、その日の`content_html`を持つ day オブジェクトに`transport_note`フィールドを追加する。`pre_transit`と同じ形式のオブジェクト（`{"duration": "KL→マラッカ→KL（バス 往復約4時間）"}`のようにアイコンフラグも指定可）で、説明文の下に矢印なしのアイコン付きボックスで表示される。
- **拠点となる都市が変わる移動で、かつ午前中に移動する場合**（例: 台北で前泊→翌朝花蓮へ）: その日（到着日）のdayオブジェクトに`pre_transit`フィールドを追加する。`{"duration": "台北→花蓮（台鉄特急 約2〜2.5時間）", "local_train": true}`のように、通常の`transit`行と同じ`duration`・アイコンフラグ（`train`/`local_train`/`taxi`/`plane`/`ferry`/`walk`）を持つオブジェクトを渡すと、そのdayの`tl-badge`（Day番号）直後・説明文の直前に矢印なしで表示される。アイコンは`generate.py`側で自動解決されるので、フラグを立てるだけでよい。
- **拠点となる都市が変わる移動で、午後に移動する場合**（例: 台南を観光してから高雄へ移動）: 従来通り`{"transit": true, "duration": "...", "train": true}`を前の日の直後・次の日の直前に独立行として挟む（矢印付き、次の日のブロックの外側）。

判断基準: 前後の`transit`行の`duration`が「その日の観光と地続きの移動」なのか「宿泊地を移す移動」なのかで使い分ける。台湾プランAの「台北→十分→九份」はDay3のうちに完結する日帰り移動なので`transport_note`に変更済み（2026-07）。

### 移動日（transit）のアイコン指定

`day.transit: true`の行は`plane`/`taxi`/`ferry`/`walk`/`train`（新幹線・高速鉄道のイラスト）/`local_train`（🚆絵文字、在来線・普通列車用）のいずれかのフラグを立てる。`train`は新幹線・KTX・Afrosiyob高速鉄道など**明示的に高速鉄道と呼べるものだけ**に使い、それ以外の在来線（台湾の台鉄、スリランカの高原鉄道など）は`local_train`を使うこと。どのフラグも立てない場合はバスアイコンがデフォルトになる。テンプレート内でこの分岐は`stable_plans`用と`adventure_plan`用で**2箇所に重複定義**されているため、`country_template.html`側を修正する場合は必ず両方に同じ変更を反映すること（`{% if day.ferry %}`で検索すると2件ヒットするはず）。

### `courses.intro` の一文テンプレート

`courses.intro`（モデルコースタブ冒頭の説明文）は、以下の型を踏襲しつつ、各国の実情（直行便時間・時差・プラン名・プラン内容）に合わせて言い換える。

```
[直行便時間・時差など基本情報]の[国名]は「[その国らしい旅の強み]」のが最大の強み。
初めてなら[エリア]に絞った<strong>[プラン1名]</strong>、
[2回目以降/連休が取れるなら]は[内容]を巡る<strong>[プラン2名]</strong>がおすすめ。
（adventure_planへの一言布石）
```

台湾の実例：
> 直行便3〜4時間・時差1時間の台湾は「短い休みでも成立する」のが最大の強み。初めてなら台北＋九份に絞った**週末プラン**、2回目以降や連休が取れるなら新幹線で西部を巡る**縦断プラン**がおすすめ。それでも物足りない！という人向けに、少し難易度の高いプランも紹介しています。

**ポイント**:
- プラン1・プラン2を紹介した後、`adventure_plan`（冒険枠）への一言布石を入れる。ただし**この一文を定型文として固定しない**こと。「それでも物足りない！という人向けに〜」は台湾での一例に過ぎず、国ごと・`adventure_plan`の内容ごとに書き分ける（例：実体験ルートがある国なら「管理人が実際に旅したルートも公開しています」、サファリが絡むなら「もっと欲張りたい人にはサファリ付きルートも」など、その都度adventure_plan自体の性格に合わせて自然な言い回しを考える）。
- 冒頭の「強み」の一言（台湾＝短い休みでも成立する、等）も国ごとに変える。直行便が少ない／遠い国（スリランカ・南アフリカ）なら「便数は少ないが現地移動はコンパクト」のような、その国ならではの利点に置き換える。
- 文字数・トーンは他国の既存introと揃える程度でよく、完全な定型文にする必要はない。
- 「どちらもアレンジ自由です」のような締めの一文は蛇足なので入れない。プラン紹介とadventure_planへの布石で終わってよい。

## `index_card.catch`（国一覧カードのキャッチ文）

`generate.py`は60文字を超えた場合にのみ切り詰める（`assets/tools/generate.py`の`update_index()`）。実際の目安は**44〜54文字**で書くこと。トーンは辞書的なキーワード列挙（例:「グルメ・世界遺産・バイクの喧騒」）ではなく、**管理人の一人称的な感想・体験談**（例:「管理人が学生の頃初めて一人旅した国だ。海外なのに安心感があるのは何故だろう。」）。未生成国のプレースホルダー（ベトナム・バリ等）は暫定の短いキーワード列挙のままなので、新規国を作る際はそちらを絶対に踏襲しないこと（韓国が最初これをやってしまい25文字の定型キャッチのまま残っていた）。`index.html`の`COUNTRIES`配列に手書きされているキャッチ文が実際にサイトに表示される値で、`<country>.json`の`index_card.catch`は初回`update_index()`実行時にコピーされるだけ（以後は連動しない）ため、後から直す場合は**両方**を書き換える必要がある。

## `spot_points`（観光のポイント）の画像

`spot_points[]`の各要素（`title`/`body`）に画像を付けたい場合、`image`パスを手打ちしない。代わりに参照先の`num`を`spot_ref`（`spot_sections`内のスポット）または`food_ref`（`food_items`内の料理）として書いておく：

```json
{ "title": "...", "body": "...", "spot_ref": "SAM 01" }
```

`generate.py`が生成のたびに、参照先スポット/料理の`image`フィールド（画像生成済みなら自動検出で入っている）を`spot_points[].image`へ自動でコピーする。イラストがまだ無い段階で`spot_ref`だけ書いておけば、後でRecraftツールから該当スポットの画像を生成した瞬間、次の`generate.py`実行で観光のポイント側にも自動反映される（手動でパスをコピペする必要がない）。

## `basic_data`（基本データ）に必須の項目

`basic_data[]`には**「通貨」の行を必ず入れる**（`_country_template.json`にも標準項目として追加済み）。`value`は通貨名と略称（例:「台湾ドル（TWD、新台幣）」）、`note`は円換算の目安（例:「1TWD ≈ 5.1円」、`overview.currency_rate`と揃える）。**「約」は不要**（「≈」記号自体が目安であることを示しているため冗長）。同じレートを`practical.prac_cards`の「通貨」カード（`旅の準備`タブに表示）にも`"{通貨名}　{レート}"`の形で追記する（例:「台湾ドル（TWD/元）　1TWD ≈ 5.1円」）。8か国すべて2026-07に追記済みだが、新規国を作る際に忘れやすいので注意。

## `overview.flag_html`（国旗グラフィック）の作り方

`overview.flag_html`は基本データタブに表示される国旗グラフィック。**絵文字（🇰🇷など）は使わない** — Windows環境で正しく描画されない（国コードの2文字表示になる）ため、全端末で見た目が揃わない。

**2026-07方針転換: 座標を自分で計算・推測するのは禁止。** 当初はWebSearchで公式仕様を調べてPythonで座標計算する方式を取っていたが、太極旗の角度を2回間違える等、検証を挟んでも実物とズレる問題が繰り返し発生した（ユーザー: 「生成すると実際の国旗と違う点が多くみうけられます」）。**必ず以下の手順で、実在する検証済みSVGデータをそのまま流用すること。**

1. パブリックドメインの[`hampusborgos/country-flags`](https://github.com/hampusborgos/country-flags)（Wikimedia Commons由来、**各国の公式縦横比をそのまま維持**）から、対象国のISO 3166-1 alpha-2コード（例: 韓国=kr, 台湾=tw, マレーシア=my, シンガポール=sg, ウズベキスタン=uz, 南アフリカ=za, スリランカ=lk, タイ=th）でSVGを取得する:
   ```
   curl -s "https://raw.githubusercontent.com/hampusborgos/country-flags/main/svg/{cc}.svg"
   ```
   （注: `lipis/flag-icons`は全国旗を強制的に4:3枠に収める仕様のため、公式比率と異なる国が多い。比率を優先するなら必ずこちらを使うこと。）
2. 取得したSVGの`viewBox`から縦横比を計算し、高さを`100px`に固定して幅を比率通りに算出する（例: 3:2の国旗なら`width="150" height="100"`、2:1の国旗なら`width="200" height="100"`）。SVGの中身（座標・path）は**一切書き換えない**。
3. `<div style="display:inline-block;border:1px solid #eee;line-height:0;overflow:hidden">`で包んでそのまま`overview.flag_html`に入れる。
4. 反映後は`mcp__visualize__show_widget`で実際に描画確認してから完了とする（ライブラリのデータなので通常は一発でOKになるはずだが、貼り付けミスがないかの確認は必ず行う）。

このライブラリに存在しない対象（架空の地域・特殊事情がある場合）に限り、WebSearchで公式仕様を調べてPythonで座標計算する旧方式にフォールバックする。その場合も、角度や位置を出したら`atan2`等で数式検証してからプレビューに進むこと（勘での微調整はしない）。
