# World Mappy — 執筆・生成ガイド

海外情報サイト「World Mappy」の国別ページに関する運用ルール。技術的なビルド手順は `assets/tools/generate.py` のdocstring・`assets/tools/_country_template.json`（新規国のスキーマ雛形）を参照。ここでは**テンプレートのコードが強制しない、執筆上の慣習**をまとめる。

> **新規国の`courses`（モデルコース）・`phrases`の設計方針は`.claude/skills/new-country-page/SKILL.md`に移動済み。** 新しい国のページを書き起こすときはそのスキルを参照する。既存ページのcourses/phrasesを軽微に直すだけなら以下の常時ルール（廃止済みフィールド・transitアイコン重複箇所）だけ押さえておけばよい。

### `days[].badge_class`（Dayバッジの色）は廃止済み

2026-09削除。`days[].badge_class`（`""`/`"green"`/`"red"`/`"gray"`）でDay番号バッジを緑・赤に塗り分けていたが、**赤の付け方が国ごとにバラバラ**（スペイン=緑赤交互で見た目のリズム、韓国=帰国日だけ赤、イタリア=丸一日観光の日を赤…）で、凡例もなく初見で意味不明だったため、`country_template.html`から`{{day.badge_class}}`を削除し**全バッジを緑に統一**した（`.tl-badge.red`のCSSも削除、全22 course.htmlから`tl-badge red/green/gray`を除去済み）。新しい国のJSONで`badge_class`を書いても無視される（書かなくてよい）。

### 移動日（transit）のアイコン指定

`day.transit: true`の行は`plane`/`taxi`/`ferry`/`walk`/`train`（新幹線・高速鉄道のイラスト）/`local_train`（🚆絵文字、在来線・普通列車用）のいずれかのフラグを立てる。`train`は新幹線・KTX・Afrosiyob高速鉄道など**明示的に高速鉄道と呼べるものだけ**に使い、それ以外の在来線（台湾の台鉄、スリランカの高原鉄道など）は`local_train`を使うこと。どのフラグも立てない場合はバスアイコンがデフォルトになる。テンプレート内でこの分岐は`stable_plans`用と`adventure_plan`用で**2箇所に重複定義**されているため、`country_template.html`側を修正する場合は必ず両方に同じ変更を反映すること（`{% if day.ferry %}`で検索すると2件ヒットするはず）。

## `index_card.catch`（国一覧カードのキャッチ文）

`generate.py`は60文字を超えた場合にのみ切り詰める（`assets/tools/generate.py`の`update_index()`）。実際の目安は**44〜54文字**で書くこと。トーンは辞書的なキーワード列挙（例:「グルメ・世界遺産・バイクの喧騒」）ではなく、**その国の情景が浮かぶ描写的な一文**にする。2026-07改訂: 以前は「管理人の一人称体験談」（例:「管理人が学生の頃初めて一人旅した国だ。」）と規定していたが、行ったことのない国も今後多く扱うため一人称の実体験を装う書き方はしない。「〜な国」「〜が待っている」のような**説明口調・図鑑的な言い回しも避け**、情景・匂い・音・光景を連想させる描写で締める（例:「メコン川と古都ルアンパバーンが残る、東南アジア最後の秘境」より「托鉢僧の列と朝もやが溶け合う、メコン河畔の静かな古都」のように具体的な情景を一つ差し込む）。**体言止めで終わらせない**——名詞で終える書き方（「〜が響く密林」「〜揺れるビーチ」等）も避け、動詞・述語で終わる一文にする（例:「托鉢僧の静かな列と川面にたつ朝もやが、メコン河畔にたたずむ古都ルアンパバーンの朝をそっと包み込む」）。**締めの動詞を`COUNTRIES`配列内で使い回さない**——「響き渡る」「溶け合う」「重なり合う」等を複数カードで繰り返すと単調になる。新しいカードを追加する際は既存の全`catch`の文末動詞を確認し、被らない動詞（漂う/揺れる/通り過ぎる/満ちる/包み込む/沸き立つ/こだまする/弾ける 等）を選ぶこと。**準備中（`available:false`）のプレースホルダーも例外ではなく、本番と同じ44〜54文字・描写トーンで書く**（旧仕様の「プレースホルダーは暫定の短いキーワード列挙のままでよい」は2026-07に撤回。ベトナム/バリ等の既存プレースホルダーも2026-07にこのトーンへ書き直し済み）。`index.html`の`COUNTRIES`配列に手書きされているキャッチ文が実際にサイトに表示される値で、`<country>.json`の`index_card.catch`は初回`update_index()`実行時にコピーされるだけ（以後は連動しない）ため、後から直す場合は**両方**を書き換える必要がある。

## `prompt_en`（イラスト生成プロンプト）の書き方

`spot_sections[].spots[].prompt_en`・`food_items[].prompt_en`はRecraft画像生成（`assets/tools/app.py`）にそのまま渡される英語プロンプト。スタイル自体はRecraft側のstyle_idで制御されるため、プロンプト本文には**時間帯**と**人物の入れ方**を意識して書くこと。

- **時間帯**: そのスポットの性質に合わせて自然文で指定する。絶景・記念碑系は`at golden hour`／`at sunset`でドラマチックに、市場・美術館・屋外散策系は`in the morning light`や指定なし（昼想定）、ナイトマーケット・バー街系は`at night`／`at dusk`。
- **人物**: 賑わいを伝えるために人物を入れるのは歓迎だが、**特定の1人を主役にして表情・手の動き・複雑なポーズまで指定すると顔や手が破綻しやすい**。安全なのは「小さく・複数・匿名・単純な動作」の組み合わせ：
  - `a few tourists in the distance` / `small figures for scale`
  - `silhouettes of visitors`（逆光・夕景と相性が良く、顔を描写せずに済む）
  - `crowds of people walking`（個人を特定させない群衆表現）
  - `a tiny hiker figure visible on the trail`（遠景で豆粒サイズ）
  - 職人の単純な動作（例: グラスブロワーが炉の前で作業している）は比較的安全
  - 避けるべき: クローズアップでの人物指定、「〜のポーズを取る」「微笑みながら〜を持つ」等の具体的な表情・手の動き指定

### `hero_image`（トップのヒーロー画像）の人物有無

`hero_prompt`に人物を入れるかどうかは、そのヒーロー画像が「風景」寄りか「観光スポット」寄りかで判断する。

- **人物なしが向く**: テーブルマウンテンの空撮、サグラダファミリアの引きの構図など、建築物・地形そのものが主題で、俯瞰・引きのアングルのもの。人物を入れる余地が構図的にない、あるいは無理に入れると不自然になる。
- **人物ありが向く**: コロッセオのような「常に観光客で賑わっている定番スポット」を地上アングルで描く場合。実際の現地の賑わいに忠実になり、絵としても生き生きする。台湾（九份でランタンに火を灯す地元の店主）・タイ（托鉢する僧侶の行列）のように、1人〜数人の単純な動作を添える。

判断に迷う場合は、上記の`prompt_en`の人物ルール（小さく・複数・匿名・シルエット、または単純な動作を1人）をそのまま適用すればよい。

## `spot_sections[].spots[].badge` / `food_items[].badge` の文字数

**10文字以内**（2026-09制定）。カード上部の緑のピルバッジ（`.badge`）は1行で収める前提のデザインで、11文字以上だと2行に折り返してレイアウトが崩れる（スペイン No.25「バレンシア名物の冷製ドリンク」＝13文字で発覚）。一言でそのスポット/料理の性格が分かる短い語にする（例:「世界遺産」「展望台」「入場無料」「発泡ワイン」「家庭の味」）。`check_rules.py` が11文字以上を NG として検出する。

## `spot_points`（観光のポイント）の文字数・件数

`body`の文字数はテンプレート側で制限されていない（`.sp-point-text`にline-clampやmax-heightの指定なし）ため、短く書いても表示上はエラーにならないが、他国と比べて情報量が薄く見えてしまう。**目安は120〜250文字程度**（既存国の実績は130〜450文字）。一文で終わらせず、背景情報や体験の具体性（何がどう凄いのか、どう楽しむべきか）を含めて2〜3文で書くこと。

> **件数を3個に固定する方針・`food_ref`を最低1個含める方針は`.claude/skills/new-country-page/SKILL.md`を参照。**新しい国を書き起こすとき、または項目を追加・入れ替えるときはそちらの方針に従う。

## `season.cities[].note`（ベストシーズンタブの気候説明）の文字数

こちらもテンプレート側の文字数制限はない。**目安は100〜160文字程度**（既存国の実績は97〜305文字）。「暑い」「過ごしやすい」で終わらせず、具体的な気温感覚・湿度・地域特有の気候現象（霧、シロッコ、アクア・アルタ等）や旅行への実務的な影響（防寒着が要る、リゾート地は混雑する等）を含めて書くこと。

**`season.cities[]`は`cities[]`の全都市を網羅する必要はない**（2026-07方針明確化）。主要都市だけに絞ってよく、気候が似ている都市同士は1エントリにまとめてよい。国土が広く地域ごとに気候差が大きい国（イタリア等）に限らず、一般的な方針として適用する。まとめる場合は`name`に代表都市名を併記する（例:「北イタリア（ミラノ・ヴェネツィア・トリノ等）」、「パクセー・チャンパーサック（ボラベン高原）」）。

### 統合可否の判断基準（数値だけで判断しない）

2026-07に、ドイツ（フランクフルト）・北朝鮮（開城）・ベトナム（ハイフォン・カントー・フーコック島）の3か国で「`note`本文に自分で『気候はほぼ同じ』と書いていながら、別エントリのまま統合し忘れている」ケースが見つかった。逆に、全16か国の`months[]`気温データを数値比較したところ、マレーシア（KL/ペナン/コタキナバル/ランカウイ）やフィリピン（マニラ/セブ/パラワン/シアルガオ）のように**平均気温はほぼ同じでも統合すべきでない国**も多数あった——これらは乾季・雨季やモンスーン・台風の**時期が地域で逆転している**ため、気温グラフだけを見ると似ていても実務上は全く別の情報になる（シアルガオの note には「太平洋側なので西部リゾートと季節が逆」と明記されている）。

**How to apply:** 統合を検討する際は以下の順で判断する。
1. `note`本文が既に「気候はほぼ同じ」「〇〇と同様」と自認しているか → 統合する（機械的な見落としの可能性が高い）。
2. 月別気温データが近い（平均差2〜3°C以内）場合でも、**乾季・雨季/台風シーズンの「時期」が地域で異なると書かれていないか**を必ず確認する。時期がズレている・逆転している記述があれば、気温が近くても統合しない（旅行計画上は別物として扱うべき情報のため）。
3. 気温も近く、季節パターンの記述にも実質的な差がない（片方がもう片方の劣化コピーのような内容になっている）場合のみ統合する（例: スペインのバレンシアはバルセロナとほぼ同じ気候・同じ季節パターンだったため統合済み）。

## `transport_items[].desc`（現地交通ガイド）の文字数

**目安は50〜80文字程度**（2026-07、マレーシア・タイ・ウズベキスタン・南アフリカ・台湾・シンガポールの実績に基づき確定）。移動手段の特徴・料金感・利用シーンが一言で伝わる程度の分量にする。

## その他の文字数ルール（2026-07、マレーシア・タイ・ウズベキスタン・南アフリカ・台湾・シンガポールの実績に基づき確定）

- `overview.description_html`（基本情報タブ冒頭の国紹介文）: **140〜190文字程度**
- `overview.top_spot_name`に対応する`top_spot_desc`（「代表的な観光地」の一言紹介）: **35〜60文字程度**
- `overview.top_food_name`に対応する`top_food_desc`（「代表的なグルメ」の一言紹介）: **50〜70文字程度**
- `overview.japan_popularity_html`（「日本人に人気の理由」）: **85〜140文字程度**
- `budget.intro`（予算タブ冒頭の導入文）: **100〜180文字程度**
- `practical.money_intro`（予算タブ「両替・支払い」セクションの冒頭説明）: **100〜180文字程度**（2026-07制定。従来ルール未整備で50〜167字とバラついていたため`budget.intro`と同じ目安に統一）
- `practical.flight_lead`（予算タブ「航空券の予約」セクションの冒頭説明）: **100〜180文字程度**（2026-07制定。従来ルール未整備で、カナダ・ドイツ・NZ等が実質空（5〜12字）、南アフリカが215字と極端にバラついていたため`budget.intro`と同じ目安に統一）。**特殊枠**: 北朝鮮のように直行便が存在せず個人渡航が制限される国は、この文字数に無理に合わせず実情に即した短文でよい。
- `practical.hotel_intro`（予算タブ「ホテルの選び方」セクションの冒頭説明）: **100〜180文字程度**（2026-07制定。詳細は下の専用節を参照）
- `budget.savings_tips_html`（予算タブ末尾「節約のコツ」）: **50〜90文字程度**
- `season_mini.description`（トップページの季節ダイジェスト文）: **35〜60文字程度**

> **`practical.hotel_intro`の書き方方針（治安配慮の織り込み方・国別の切り口）は`.claude/skills/new-country-page/SKILL.md`参照。** 文字数だけは常時ルール: 100〜180字程度。空欄の国は自動的に旧固定文が表示されるので、新規国では必ず書き下ろすこと。

## `budget.items[].detail_html`（予算タブの費用内訳）の行数・文字数

`<br>`区切りで**2行固定**が基本（2026-07、マレーシア・タイ・ウズベキスタン・南アフリカ・台湾・シンガポールの実績に基づき確定。6か国36項目中、3行になっているのはマレーシア・タイの「食費」項目のみの例外）。各行の目安は**15〜60文字程度**。1行目に内訳・選択肢、2行目に価格変動のコツや注意点を書く構成が定番。

### `交通費（1日）`に都市間移動（国内線・レンタカー）のコストを混ぜない

`budget.items`には既に「航空券（往復）」という独立項目があるため、`交通費（1日）`の価格レンジ・`plan_rows`の交通行には**都市間移動（国内線・レンタカー）のコストを含めない**——航空券という別枠があるのに交通費側にも大口の移動費を計上すると、同じ種類の出費（飛行機移動）が二重に予算に乗ってしまう。`交通費（1日）`はあくまで滞在中の**都市部の日常的な移動**（バス・地下鉄・タクシー・配車アプリ）だけを反映すること。

2026-07にカナダ・NZの`交通費（1日）`が「都市間の移動は国内線かレンタカーが基本」という説明のまま、`plan_rows`の標準・贅沢欄に「レンタカー込み4,500〜7,000円」「国内線・専属ドライバー12,000円〜」のように大口移動費をそのまま日割りで計上していたのが発覚し、標準は「バス+タクシー併用」、贅沢は「配車アプリ・タクシー中心」という都市部の移動だけの金額に修正した（`total_std`/`total_lux`もこの分を差し引いて調整済み）。国内線・レンタカーが必要という情報自体は`detail_html`に「都市間移動は国内線・レンタカーが必要（航空券とは別枠）」のような注記として残してよいが、金額には含めない。

なお`practical.cash_daily_low`/`cash_daily_high`の計算式は元々、`交通費（1日）`の`detail_html`に「国内線」「レンタカー」等の都市間移動を示す語がある場合は上限を無視し**下限だけ**を使う例外ルールになっている（前掲の「`practical.cash_daily_low` / `cash_daily_high`」節を参照）。この項目自体の金額を都市部の移動だけに正しく揃えておけば、その下限（＝都市部の移動費）をそのまま使う限りcash_dailyの計算に影響はない。

## `practical.cash_daily_low` / `cash_daily_high`（旅の準備タブ「現地現金の目安」カード）の算出ルール

2026-07制定。これまでは国ごとの感覚値で手入力されており、`budget.items`の「食費（1日）」「交通費（1日）」の目安と整合しない値が多数見つかった（例: 韓国は食費+交通費だけで1日2,500〜17,250円相当が必要な計算になるのに、旧`cash_daily`は1,080〜3,240円相当しかなく大幅な過小評価だった。カナダ・イタリア・NZ・シンガポール・南アフリカ・スリランカ・ラオスでも同様の過小評価を確認・修正済み）。以後は以下の手順で機械的に算出する。

1. `budget.items`の「食費（1日）」の`price`（円のレンジ）は**中央値（下限と上限の平均）**を取る。下限（節約・屋台/バックパッカー水準の最安値）だけを根拠にしない・上限（少し贅沢した場合の最高値）だけを根拠にしない、どちらの極端にも寄せない「普通に旅行した場合の目安」がここでの狙い。
   - 「交通費（1日）」も基本は同じく中央値を取るが、**`detail_html`の上限側が「国内線」「レンタカー」「隣島船」のような都市間・地域間移動（＝カードで事前決済するのが基本で、頻繁に繰り返す日常の現金支出ではない）を指している場合は、中央値ではなく下限（＝都市部の公共交通・配車アプリなど、実際に現金が絡みうる日常の移動費）だけを使う**。上限まで含めて中央値を取ると、たまにしか使わない大口移動（レンタカー代・国内線代）が現金の目安に混入し、過大評価になるため（2026-07にカナダ・NZで発覚: 交通費が「都市間の移動は国内線かレンタカーが基本」という説明で1,200〜7,000円/日という開きがあり、上限の国内線・レンタカー代まで中央値に含めていたため`cash_daily`が実測値の約1.3〜1.5倍に膨らんでいた）。
   - ホテル代・航空券のような大口決済（カード払い・事前予約が基本）はこの合算に含めない、という原則自体は変わらない——交通費の例外もこの原則の延長。
2. **`practical.cash_dependency`（現金依存度）に応じて、その金額に係数を掛ける**——食費・交通費のうちどの程度が実際に現金払いになるかは、キャッシュレス化の進み具合で大きく変わるため（カード・タッチ決済が主流の国でそのまま現金の目安にすると過大評価になる）。
   - `低`（カード・タッチ決済がほぼ通用、韓国・カナダ・NZ・シンガポール・南アフリカ・イタリア・スペイン・マレーシア等）: **×0.2**
   - `中`（都市部はカードOKだが屋台・市場等は現金中心、ドイツ・タイ・台湾・ベトナム・スリランカ・フィリピン等）: **×0.7**
   - `高`（現金がないと日常的に困る、ラオス・ウズベキスタン等）: **×1.0**
3. `practical.exchange_rate`（1現地通貨単位あたりの円換算）で現地通貨に変換した値を`cash_daily_low`とする。
4. **現金は多めに持って困ることはない**ため、`cash_daily_high`はさらに3割ほど上乗せして切り上げる（両替忘れ・予定外の出費に備える趣旨。それでも尚不足する場合に備えて、`money`セクションでは現地ATMで引き出せるWiseカードの紹介も併記しているため、`cash_daily`自体を過剰に大きく取る必要はない）。
5. 現地でよく流通する紙幣単位に合わせてキリの良い数字に丸める（例: 韓国はウォン紙幣なので1000/10000単位、ベトナムは50000/100000単位、ヨーロッパ諸国のユーロは5/10単位）。

**この計算式は全国共通で機械的に適用する**——特定の国だけ実測値・現地情報ベースの数値に個別上書きする運用はしない（2026-07にドイツで実測値ベースの上書きを試みたが、ユーザーから「現地情報ベースは無くして」と指摘され撤回。計算式自体の精度を上げることで対応する方針に統一）。新しい国を作る・見直す際は、`budget.items`の食費・交通費と`cash_dependency`が決まった時点でこの計算に基づいて`cash_daily_low`/`cash_daily_high`を機械的に埋めること（感覚で決め直さない）。2026-07に全16か国（北朝鮮を除く）をこの式で再計算・修正済み。

**方式の変遷（2026-07）**: (1) 当初は食費+交通費の下限〜上限レンジをそのまま使う案 → (2) ユーザーから「現金依存度にもよるやろ」と指摘され依存度係数を追加 → (3) ユーザーから「節約の費用リストは使わないでほしい、標準以上を現金目安に使いたい」と指摘され、上限のみを根拠にする式に変更 → (4) ドイツの実測値（5日間・カードメイン利用で1人100〜150ユーロ＝1日20〜30ユーロ）と比べ、上限のみを根拠にした式の値（40〜50ユーロ/日）が過大だったため、根拠を上限のみから**中央値**に変更 → (5) カナダで「現金目安高くね？」と指摘があり検証したところ、`交通費（1日）`が「都市間は国内線かレンタカーが基本」という説明で1,200〜7,000円/日と開きがあり、上限（国内線・レンタカー代）まで中央値に含めていたのが原因と判明。交通費の上限が都市間移動を指す国では交通費だけ下限を使う例外を追加した（カナダ・NZ・南アフリカ・ベトナム・フィリピンで該当）→ (6) ユーザーから「カード依存度高い国は0.2倍にしよう」と指摘され、`低`の係数を0.4→0.2に変更 → (7) 続けて「現地情報ベースは無くして」と指摘され、ドイツの実測値による個別上書きを撤回し、全国共通の計算式のみで運用する方針に確定した。

## `spot_points`（観光のポイント）の画像

`spot_points[]`の各要素（`title`/`body`）に画像を付けたい場合、`image`パスを手打ちしない。代わりに参照先の`num`を`spot_ref`（`spot_sections`内のスポット）または`food_ref`（`food_items`内の料理）として書いておく：

```json
{ "title": "...", "body": "...", "spot_ref": "SAM 01" }
```

`generate.py`が生成のたびに、参照先スポット/料理の`image`フィールド（画像生成済みなら自動検出で入っている）を`spot_points[].image`へ自動でコピーする。イラストがまだ無い段階で`spot_ref`だけ書いておけば、後でRecraftツールから該当スポットの画像を生成した瞬間、次の`generate.py`実行で観光のポイント側にも自動反映される（手動でパスをコピペする必要がない）。

## `basic_data`（基本データ）に必須の項目

`basic_data[]`には**「通貨」の行を必ず入れる**（`_country_template.json`にも標準項目として追加済み）。`value`は通貨名と略称（例:「台湾ドル（TWD、新台幣）」）、`note`は円換算の目安（例:「1TWD ≈ 5.1円」、`overview.currency_rate`と揃える）。**「約」は不要**（「≈」記号自体が目安であることを示しているため冗長）。同じレートを`practical.prac_cards`の「通貨」カード（`旅の準備`タブに表示）にも`"{通貨名}　{レート}"`の形で追記する（例:「台湾ドル（TWD/元）　1TWD ≈ 5.1円」）。8か国すべて2026-07に追記済みだが、新規国を作る際に忘れやすいので注意。

## `overview.flag_html`（国旗グラフィック）の作り方

`overview.flag_html`は基本データタブに表示される国旗グラフィック。**絵文字（🇰🇷など）は使わない** — Windows環境で正しく描画されない（国コードの2文字表示になる）ため、全端末で見た目が揃わない。

**2026-07方針転換: 座標を自分で計算・推測するのは禁止。** 当初はWebSearchで公式仕様を調べてPythonで座標計算する方式を取っていたが、太極旗の角度を2回間違える等、検証を挟んでも実物とズレる問題が繰り返し発生した（ユーザー: 「生成すると実際の国旗と違う点が多くみうけられます」）。**必ず以下の手順で、実在する検証済みSVGデータをそのまま流用すること。**

1. パブリックドメインの[`hampusborgos/country-flags`](https://github.com/hampusborgos/country-flags)（Wikimedia Commons由来、**各国の公式縦横比をそのまま維持**）から、対象国のISO 3166-1 alpha-2コード（例: 韓国=kr, 台湾=tw, マレーシア=my, シンガポール=sg, ウズベキスタン=uz, 南アフリカ=za, スリランカ=lk, タイ=th）でSVGを取得する:
   ```
   curl -s "https://raw.githubusercontent.com/hampusborgos/country-flags/main/svg/{cc}.svg"
   ```
   （注: `lipis/flag-icons`は全国旗を強制的に4:3枠に収める仕様のため、公式比率と異なる国が多い。比率を優先するなら必ずこちらを使うこと。）
2. 取得したSVGの`viewBox`から縦横比を計算し、高さを`100px`に固定して幅を比率通りに算出する（例: 3:2の国旗なら`width="150" height="100"`、2:1の国旗なら`width="200" height="100"`）。SVGの中身（座標・path）は**一切書き換えない**。
3. `<div style="display:inline-block;border:1px solid #eee;line-height:0;overflow:hidden">`で包んでそのまま`overview.flag_html`に入れる。
4. 反映後は`mcp__visualize__show_widget`で実際に描画確認してから完了とする（ライブラリのデータなので通常は一発でOKになるはずだが、貼り付けミスがないかの確認は必ず行う）。

このライブラリに存在しない対象（架空の地域・特殊事情がある場合）に限り、WebSearchで公式仕様を調べてPythonで座標計算する旧方式にフォールバックする。その場合も、角度や位置を出したら`atan2`等で数式検証してからプレビューに進むこと（勘での微調整はしない）。

## `overview.visa`（基本情報タブ「ビザ」カード）の文言統一

2026-07制定。表記ゆれ（「不要（90日）」「不要（90日以内）」「90日以内の観光は査証免除」等）と、ETIAS/NZeTA/ETAのような略称がいきなり本文に出てくる分かりにくさ（ユーザー: 「ETIASとかETAが何かわからないから、書き方考えたい」）を解消するため、以下の3パターンに統一する。

- **パターンA（ビザ不要）**: `不要（○日）` — 例:「不要（90日）」。「申請不要」のような冗長な補足語は付けない。
- **パターンB（電子渡航認証が必要）**: `オンライン申請要（○日）。渡航前に{制度名}（{日本語の説明}）への登録が必要です` — 例: ドイツ/イタリア/スペイン「オンライン申請要（90日）。渡航前にETIAS（欧州渡航認証）への登録が必要です」、NZ「...NZeTA（電子渡航許可）...」
- **パターンC（電子ビザの取得が必須）**: `{取得種別}取得要（○日）。{制度名}（{日本語の説明}）をオンラインで事前取得（条件）` — 例: スリランカ「電子ビザ取得要（30日）。ETA（電子渡航認証）をオンラインで事前取得（手数料無料）」
- **特殊枠**: 北朝鮮のように制度が根本的に異なる国（個人取得不可・団体ツアー限定等）は上記に当てはめず、実情に即した短文のまま。

**ポイント**: 太字本文（句点より前）には**略称を置かない**——「何をすればいいか」を平易な日本語だけで完結させる（「オンライン申請要」「電子ビザ取得要」等）。略称とその日本語訳は句点より後ろの補足部分に回す（下記「カード説明文の自動分割」の仕組みで自動的に小さいサブテキストとして表示される）。シェンゲン協定加盟国（ドイツ・イタリア・スペイン等）は全てETIAS必須のためパターンBで足並みを揃えること。今後シェンゲン加盟国を追加する場合も同様。

## カード説明文の自動分割（`p-val-sub` / `qs-val-sub`）

2026-07追加。`practical.prac_cards[].value`（旅の準備タブ）と`overview.visa`（基本情報タブのクイックカード）は、`assets/tools/generate.py`の`generate()`内にある`_split_long_value()`により**表示時に自動整形**される：値に「。」が含まれ、句点以降の残り文字数が4文字以上なら、句点より前を太字本文、それ以降を小さいグレーの補足テキスト（`.p-val-sub`/`.qs-val-sub`、`assets/style.css`で定義）として下に配置する。**太字本文側の句点「。」自体は表示されない**（区切り位置の判定にのみ使い、`head = val[:idx]`で句点を含めずに切り出す）——句点が行末に浮いて見えるとの指摘（2026-07）を受けて変更。

**JSON側はHTMLを書かず、普通の日本語の文（句点区切り）を書くだけでよい**——`<span class="p-val-sub">`等を手で埋め込む必要はなく、`generate.py`実行のたびに自動適用される（JSON自体は書き換わらない）。新しい国のカード説明文が長くなりそうな場合はこの仕組みに任せる。

## `overview.flight_hours`（基本情報タブ「直行便」カード）の文言統一

- **直行便がある国**: `直行便で約○時間` — 乗継便も選択肢としてある場合は末尾に`（乗継便あり）`を付ける（例:「直行便で約7時間（乗継便あり）」）。乗継便の具体的な所要時間が分かっている場合のみ`（乗継便は約○〜○時間）`のように数値を書く（例: イタリア「直行便で約13時間（乗継便は約14〜17時間）」、ウズベキスタン「直行便で約10〜11時間（乗継便は約13〜14時間）」）。根拠のない数値を推測で書かない。
- **直行便がない国**: `乗継で約○時間` — 経由地の都市名・航空会社名は書かない（例: ラオス「乗継で約9〜13時間」）。
- **特殊枠**: 北朝鮮のように直行便自体が存在しない国は`直行便なし`のみでよい。

## `overview.timezone`（基本情報タブ「時差」カード）の文言統一

- 時差がある国: `日本 −N時間`（「日本」と符号の間に半角スペースを1つ入れる）。サマータイム制度がある国は、括弧書きではなく**句点で区切って**`サマータイム時は−N時間`を続ける（例: ドイツ「日本 −8時間。サマータイム時は−7時間」）。こうすると「カード説明文の自動分割」の仕組みで基本時差が太字・サマータイム情報が小さいサブテキストとして表示される（2026-07に括弧書きから句点区切りに変更）。プラス方向の時差は`日本+N時間`のように符号をそのまま使う（例: NZ「日本+3時間。サマータイム時は+4時間」）。
- 時差がない国: `時差なし`のみ（「（日本と同じ標準時）」のような冗長な補足は付けない）。
- `overview.timezone`も`overview.visa`と同じく`generate.py`内の`_split_long_value()`で自動分割される（`qs-val-sub`クラス）。サマータイムがない国は句点を入れる必要がないので単に`日本 −N時間`のままでよい。

## `overview.budget_range`（基本情報タブ「旅行費用目安」カード）の文言統一

`○〜○万円`のみを書く。特定の泊数（例:「（3泊4日）」）を書き添えない——他国と前提が揃わず比較できなくなるため。北朝鮮のような特殊事情がある国のみ、実情に即した補足を残してよい。

## `overview.difficulty_desc`は廃止済み（新規国のJSONに書かない）

2026-07削除。「旅行難易度」カード（`difficulty_label`/`difficulty_pct`/`difficulty_bar`でバーとバッジを描画）用に各国のJSONへ長文の説明を書いていたが、`assets/country_template.html`側でこのフィールドを参照している箇所が1つもなく、実際には**どのページにも表示されていなかった**（ユーザーが「マレーシアだけ空欄」と気づいたことで発覚）。表示されない死んだフィールドだったため、`_country_template.json`を含む全JSONから削除した。新しい国を作る際は`difficulty_label`/`difficulty_label_bg`/`difficulty_label_color`/`difficulty_pct`/`difficulty_bar`の5つだけを書けばよく、`difficulty_desc`は書かない。

### `difficulty_bar`・`difficulty_label_bg`・`difficulty_label_color`の配色ルール

2026-08制定。`difficulty_bar`（バーのグラデーション）は`difficulty_label`（バッジの文言）に応じて以下の4パターンに統一する。**国旗の色を流用しない**——2026-08にトルコ・インドネシアの`difficulty_bar`が国旗の赤系グラデーションになっており、「ふつう」なのに視覚的には最難関のように見えてしまう問題が発覚し、他国の慣習に合わせて修正した実績あり。

| `difficulty_label` | `difficulty_bar`（グラデーション） | `difficulty_label_bg` | `difficulty_label_color` |
|---|---|---|---|
| やさしい | `linear-gradient(90deg,#00a86b,#66cc99)` | `#e8f5ee` | `var(--green)` |
| ふつう／普通 | `linear-gradient(90deg,#00a86b,#ffcc80)` | `#fff4e0` | `#b09000` |
| やや難しい／やや易しい | `linear-gradient(90deg,#ff9800,#ffcc80)` | `#fff3e0` | `#e65100` |
| 最難関 | `linear-gradient(90deg,#cc0001,#8a0000)` | `#fdeaea` | `#cc0001` |

`difficulty_pct`（バーの塗りつぶし割合）はラベルとは別に各国の実情に応じて0〜100の数値で設定してよいが、色自体は上記表のラベル単位の配色から外れないこと。北朝鮮（`最難関`・100%）のような特殊枠も表の配色パターンにそのまま従う。

## `manner_cards` / `manner_cta_title` / `manner_cta_desc` / `practical.cta_title` / `practical.cta_desc`は廃止済み（新規国のJSONに書かない）

2026-07削除。`difficulty_desc`と同じパターンで、`country_template.html`にも`assets/tools/generate.py`にもこれらのフィールドを参照している箇所が1つもなく、**どのページにも表示されていなかった**（マナー・エチケット紹介用の「してよいこと／してはいけないこと」カード(`type: do/dont`)として各国JSONへデータを書いていたが死んだフィールドだった）。全国のJSON・`_country_template.json`から削除済み。新しい国を作る際はこれらのフィールドを一切書かない。

なお、各タブ末尾の「次はこちらもチェック」的なCTAボックスは、`courses`タブ末尾（予算タブへの誘導）の**1箇所を除いて全てテンプレートに直書きされた固定文言**で、JSON側で国ごとに書く必要はない。国ごとに書くのは`courses.cta_title`/`courses.cta_desc`のみ。

## `overview.currency_name` / `currency_rate`（基本情報タブ「通貨」カード）の文言統一

- `currency_name`: `{通貨名}({略称})` — 括弧は必ず**半角**（`（）`全角括弧は使わない）。例:「ユーロ(EUR)」「韓国ウォン(KRW)」。
- `currency_rate`: `1{略称} ≈ {円換算}円`の型に統一する（例:「1EUR ≈ 186円」「1TWD ≈ 5.1円」）。桁が大きい通貨（ラオス・ウズベキスタン等）は`1万{略称} ≈ {円}円`、極小額通貨（ベトナム等）は`1,000{略称} ≈ {円}円`のようにキリの良い単位に丸める。「1円 ≒ 9.3ウォン」のような日本円起点の逆算表記は使わない（他国と型が揃わない）。北朝鮮のように現地通貨が実質使えない国のみ、実情に即した説明文でよい。
- レートは`assets/tools/check_exchange_rates.py`（無料為替API・open.er-api.comを使用、APIキー不要）で定期的に実勢レートと突き合わせること。このスクリプトはマレーシア・タイ・ウズベキスタン・南アフリカ・台湾・シンガポール・スリランカのみ対応済み（`COUNTRIES`配列に追記すれば対象国を増やせる）。ユーロ圏（ドイツ・イタリア・スペイン）は現状このスクリプトの対象外のため、レートが揃っているか国をまたいで目視確認すること（2026-07に3か国で173円/170円/170円とバラついていたのを発見・186円に統一した実績あり）。
- `basic_data`「通貨」の`note`は`generate.py`の`generate()`内で`overview.currency_rate`を正として自動的に上書きされる（`言語`と同じ仕組み）。そのため新しい国を作る際は`overview.currency_rate`だけ書けばよい。`value`は自動同期の対象外なので、括弧を半角にすることと、現地語の通貨名（例:「新台幣」）を追加してもよい点は手動で意識すること。

## `practical.prac_cards`（旅の準備タブ）の構成 — 10枚固定

`prac_cards`は以下の10項目・順序で統一する：ビザ／通貨／コンセント／Wi-Fi／時差／気温／チップ／水道水／治安／カード払い。

- **コンセント**には電圧情報も統合する。値は`"{プラグ形状}。{電圧}（変換注意）"`の形（句点で区切る）にすると、上記「カード説明文の自動分割」の仕組みでプラグ形状が太字・電圧情報が小さいサブテキストとして自動表示される（例: ドイツ「Cタイプ・Fタイプ（丸2穴・アース端子付き）。230V/50Hz（変換注意）」）。
- プラグ形状の後の括弧書きには**形状の説明のみ**を書く（例:「三又」「3ピン角」「丸2穴」「丸2穴・アース端子付き」）。「変換プラグ必要」「変換不要な場合も」のような対応可否の注記は書かない——電圧側の「（変換注意）」で対応要否は既に伝わっており、プラグ形状の括弧と役割が重複するため（2026-07にイタリア・スペインの「変換プラグ必要」を削除・形状表記に修正）。ただし**「Aタイプは日本と同じ」のような日本のプラグとの比較は例外的に有用な情報として残してよい**（例: フィリピン「A・Cタイプ（Aタイプは日本と同じ）」、台湾「Aタイプは日本と同じ」）。プラグ規格名は必ず実在するIEC区分（A〜O）を使うこと——2026-07にドイツの「SEタイプ」という架空表記を発見し「Fタイプ」に修正した実績あり。
- 電圧側の補足は**「（変換注意）」で統一**する（2026-07制定）。「日本の家電は変圧器が必要」のような言い換えは冗長なので使わない。例外は2か国のみ：**台湾**は110Vで日本の100Vとほぼ互換のため注記自体を付けない（「Aタイプは日本と同じ。110V」）、**北朝鮮**は220V/60Hzで他国と同じ「（変換注意）」を付ける。
- **`電圧`単独カードは廃止済み**（2026-07）。理由: 今どきのスマホ・PC充電器は大半が100〜240V対応で、電圧単体の情報は実用価値が薄く、コンセント形状と統合した方が旅行者にとって一目でわかる。
- **`治安`と`カード払い`は両方入れる**（2026-07以前は国によってどちらか一方しかなく、最後の1枠のテーマが国ごとにバラついていた）。`治安`は`overview.danger_note`を短く言い換えた内容、`カード払い`はカード決済の普及度・現金が必要な場面を書く。
- **Wi-Fi**は`"都市部は快適。{例外エリアがあれば弱点}"`の型に統一する（例: NZ「都市部・観光地は快適。山岳部は圏外エリアも多い」、スリランカ「都市部は快適。山間部は不安定」）。太字部分は一言（「〇〇は快適」）に留め、SIM/eSIM事情や地方の弱点などの詳細は句点の後ろに回す——太字を長くしすぎない（2026-07制定）。シンガポールのように国土のほぼ全域が都市部で「地方」に相当する区分がない国も、他国との統一を優先して「都市部は快適」を太字にする（実情としても矛盾しない）。北朝鮮は特殊事情（外国人向けSIM契約不可）のためこの型に当てはめず現状維持。
- **気温**は`"{ざっくり気温}。{気候の体感的な特徴}"`の型に統一する（2026-07制定）。太字は「夏○°C・冬○°C前後」「年中○°C前後」「○°C前後」のように短い代表値のみに留め、詳細レンジ（「21〜24°C」等）はサブテキストに繰り返さない。**サブテキストには気温の数値ではなく、乾燥/多湿・朝晩の寒暖差・季節が日本と逆（南半球）・雨季/スコールなど体感的な気候の特徴を書く**（例: NZ「夏21°C・冬6°C前後。季節が日本と真逆（12月が夏）、朝晩の寒暖差が大きい」、タイ「年中30°C前後。高温多湿、雨季はスコールが多い」）。太字が体言止めで終わる場合、「〜も」のような助詞で終わらせず名詞で止める（例: ウズベキスタン「冬は氷点下も」→「冬は氷点下」、ベトナム「北部は冬10°C台も」→「北部冬10°C台」）。数値自体が欠けている国（韓国は当初「四季あり」だけで具体的な数字がなかった）は、実際の気候データを調べてから埋めること——他国に合わせるために数字を推測で作らない。
- **チップ**は太字を`不要`／`任意`／`○%が目安`の3パターンのいずれかに絞る（2026-07制定）。「基本不要」「基本的に不要」のような言い換えはせず`不要`に統一し、「〜が一般的」のような具体的な慣習・金額はサブテキストに回す（例: ドイツ「5〜10%が目安。会計の端数を切り上げて渡すのが一般的」、シンガポール「不要。サービス料10%込みのため」）。付け加える情報が特にない場合は太字のみで完結してよい（例: 台湾・韓国・マレーシアは「不要」のみ）。
- **水道水**は`"{飲用可/飲用不可}。{理由・対策}"`の型に統一する（2026-07制定）。太字は「飲用可」「飲用不可」の2択のみ（「飲用可能」「飲用OK」「飲用不可推奨」のような言い換えはしない）、理由や対策（「ペットボトル推奨」「硬水で味に癖あり」等）は句点の後ろのサブテキストに回す（例: 台湾「飲用不可。ペットボトル推奨」、ドイツ「飲用可。硬水のため気になる場合はミネラルウォーターを」）。マレーシアはタイトル自体に「水道水（飲用不可）」と可否を書き値に対策だけを入れる構造異常があったため、タイトルは「水道水」に戻し値側に統一した——`title`に可否判定を書き込まないこと。
- **カード払い**は太字を`ほぼ全て利用可`／`都市部は普及`／`使用不可`の3パターンに絞る（2026-07制定）。屋台・地方・農村など現金が必要な場面の詳細はサブテキストに回す（例: 台湾「都市部は普及。夜市の屋台は現金が多い」、北朝鮮「使用不可。現金のみ」）。「◎」「○」のような記号は使わない。
- **カード払い**は太字を`ほぼ全て利用可`／`都市部は普及`／`使用不可`の3パターンに絞る（2026-07制定）。屋台・地方など現金が必要な場面の詳細はサブテキストに回す（例: ドイツ「都市部は普及。小規模店・屋台は現金のみのことも多い」、シンガポール「ほぼ全て利用可」）。北朝鮮のみ「使用不可。現金のみ」の特殊枠。
- **治安**は太字を`overview.danger_level`に対応させ`良好`（レベル0）／`十分注意`（レベル1）の2段階に統一する（2026-07制定）。「概ね良好」「治安良好」「比較的良好」のような言い換えはせず、レベル0の国は必ず`良好`、レベル1の国は必ず`十分注意`で始める。具体的な注意エリア（スリの多発地帯、国境地帯、都市名等）はサブテキストに回す（例: イタリア「良好。観光地・ターミナル駅でのスリ被害が多め」、南アフリカ「十分注意。都市部（特にヨハネスブルグ・ケープタウン）は要注意」）。北朝鮮（レベル3）のみ「個人での外出不可。常時ガイド同行」の特殊枠。
- `_country_template.json`もこの10項目構成に揃えてあるので、新しい国を作る際はこの順序・項目のまま埋めればよい。

> **`practical.special_note_*`の役割、`prep_steps`の構成順序、`tour_platforms`/`apps[]`の4社選定基準、`country_items`の書き方は`.claude/skills/new-country-page/SKILL.md`参照。** 新しい国を書き起こすときはそのスキルに従う。

**常時ルールとして残すのは以下の2点のみ**（既存ページを直す際にも影響する仕様上の注意）:
- `practical.tour_platforms`は自由記述ではなく、`assets/affiliates-data.js`の`AFFILIATE_CARDS`に定義済みのキー名（`klook`/`getyourguide`/`viator`等）を**そのまま**配列で入れる。キーと一致しない文字列を入れると中身の無い空カードが残るだけになる（テンプレート側がキー検索でカードHTMLを生成する仕組みのため）。該当なしは`[]`。
- `practical.apps[]`のうち`icon`/`affiliate`に使うキーも同じく`AFFILIATE_CARDS`のキーと一致させること。

## `overview.danger_level` / `danger_label` / `danger_note`（基本情報タブ「渡航危険情報」カード）の文言統一

- `danger_level`が0（外務省危険情報レベルなし相当）の国: `danger_label`は`特に危険情報なし`で統一する（「特に問題なし」は使わない）。
- `danger_level`が1（十分注意）の国: `danger_label`は`レベル1：十分注意`で統一する（「レベル1未満（十分注意）」「一部地域に注意」のような言い換えはしない）。
- `danger_note`（レベルの補足説明）は体言止め・言い切りで統一し、**文末に句点「。」を付けない**（他の項目が付けていないため）。空欄でよい国（本当に地域限定の注意事項がない場合）は無理に埋めない。**目安は15〜35文字程度**（2026-08制定。ウズベキスタン「アフガニスタン国境・山岳国境地帯はレベル2」が模範例）。危険地域名・都市名の列挙を欲張って長文化しない——「〇〇州××市周辺はレベル3、△△州や□□州××市・××湖等南部地域はレベル2、首都中心部や主要観光地はレベル1」のように全レベルを逐一説明するのではなく、「危険な地域はレベル2〜3、主要観光地はレベル1」程度に要約する（トルコ・メキシコ・ブラジル・インドネシアで60〜83文字の長文になっていたのを2026-08に15〜31文字へ短縮した実績あり）。
- `danger_level`の数値自体は外務省海外安全情報に基づく実情なので、レベルを揃えるために数値を動かさないこと（`label`の言い回しだけを統一する）。
- 北朝鮮のように特殊な渡航自粛事情がある国（`danger_level: 3`）は上記に当てはめず、実情に即した文章のままでよい。

## `overview.language`（基本情報タブ「言語」カード）の文言統一

`{現地公用語}（英語は{通用度}）`の型に統一する。例: ドイツ「ドイツ語（英語は観光地で広く通じる）」、ベトナム「ベトナム語（英語は都市部中心、地方は通じにくい）」。英語自体が公用語・共通語になっている国（シンガポール・南アフリカ・ニュージーランド）は例外的に「英語（公用語）」を主語にした書き方でよい（英語の通用度をわざわざ「英語は〜」と入れ子にすると不自然なため）。

## `overview.language` と `basic_data`「言語」の役割分担

`overview.language`（基本情報タブのクイックカード）と`basic_data`内の「言語」行は、同じ「言語」という話題を扱うが**書く場所を分ける**。

- `basic_data`「言語」の`value` = `overview.language`と**全く同じ文字列**（現地公用語＋「英語は{通用度}」の括弧書き）。`generate.py`の`generate()`内で`overview.language`を正として`basic_data`「言語」の`value`を自動的に上書きするため（ビザ/通貨/時差と同じ仕組み）、新しい国を作る際は`overview.language`だけ書けばよく、`basic_data`側の`value`は何を書いても生成時に上書きされる。
- `basic_data`「言語」の`note` = **言語学的な豆知識のみ**。英語の通用度は`value`側（＝`overview.language`のコピー）に既に入っているので、`note`では重複させない。文字体系・方言・声調・多言語国家である事実・言語の成り立ちなど「へえ」と思えるトリビアだけを書く（例: ドイツ「長い複合語で知られ、南部バイエルン地方の方言は標準語話者でも聞き取りづらいほど違う」、タイ「5つの声調を持つ言語で、同じ発音でも音程が違うと意味が変わる。文字は古クメール文字を起源とする独自のタイ文字」）。

2026-07制定。試行錯誤の末（英語の通用度を`note`の末尾に追記する案を経て）、ユーザーから「英語の通用度は現地公用語の横に括弧書きで記載する程度にしてください。つまり言語カードに記載していた内容をそのまま書く」と指定があり、**`value`に`overview.language`をそのまま複製し、`note`はトリビア専用に戻す**形に確定した。

## `overview.visa` / `timezone` / `currency_name`+`currency_rate` と `practical.prac_cards` の重複について

基本情報タブのクイックカード（`overview.visa`・`overview.timezone`・`overview.currency_name`+`currency_rate`）と、旅の準備タブの`practical.prac_cards`内の「ビザ」「通貨」「時差」は同じ情報を表示する重複カードだが、`generate.py`の`generate()`内で**`overview.*`を正として`prac_cards`側を自動的に上書きする**処理が入っている（該当箇所: `_prac_sync`辞書）。そのため新しい国を作る際は`overview.visa`・`overview.timezone`・`overview.currency_name`・`overview.currency_rate`だけを書けばよく、`prac_cards`内の「ビザ」「通貨」「時差」の`value`は（`_country_template.json`のプレースホルダーのままで構わない、どうせ生成時に上書きされる）二重に手入力する必要はない。逆に言うと、`prac_cards`側の「ビザ」「通貨」「時差」を個別に直しても`overview.*`と食い違えば無視されるので、修正は必ず`overview`側で行うこと。

## トップページ`COUNTRIES`配列の`tags`（国一覧カードのタグ付け）ルール

`index.html`の`COUNTRIES`配列内、各国オブジェクトの`tags:[]`（国一覧カードに表示され、タグ絞り込みチップの対象にもなる）は2026-07にルール未整備のまま運用されていたため制定。

**固定8種の「コアタグ」**（`index.html`のタグ絞り込みチップ、`toggleTag()`呼び出し箇所と完全一致する文字列でなければフィルターに引っかからない）:
`ビーチ` `世界遺産` `グルメ` `自然` `都市` `リゾート` `歴史` `アウトドア`

**方針**: `tags`には上記コアタグから**2〜4個**を必須で含め（フィルター機能で確実に拾われるようにするため）、加えてその国ならではの**個性タグを1〜2個**添える（例: 南アフリカ「サファリ」、台湾「夜市」、スリランカ「紅茶」、フィリピン「ダイビング」）。個性タグは各国の魅力を一言で伝える固有名詞・体験ワードにする——「大自然」「アドベンチャー」のようにコアタグの言い換えに見えて実は文字列が一致しない曖昧な単語は避ける（コアタグを名乗るならコアタグの文字列をそのまま使う。言い換えたい場合はコアタグ本体は別途含めた上で、言い換え語は個性タグとして追加する）。

カード表示は`tags`の**先頭4個のみ**（`c.tags.slice(0,4)`）を使うため、5個以上書く場合はコアタグ・個性タグとも見せたいものを先頭4個以内に収める。

**特殊枠**: 北朝鮮のように一般的な観光カテゴリ（コアタグ）が実情に合わない国は、この方針に当てはめずコアタグ0個・実情に即した個性タグ（史跡／特殊事情／ツアー限定／現在渡航不可）のみでよい。
